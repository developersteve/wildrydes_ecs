import { ExportResult } from '@opentelemetry/core';
import { ReadableSpan, SpanExporter } from '@opentelemetry/sdk-trace-base';
/**
 * This is implementation of {@link SpanExporter} that prints spans to a file.
 * This class can be used for debug purposes. It is not advised to use this
 * exporter in production.
 */
export declare class FileSpanExporter implements SpanExporter {
    private _fd;
    private readonly _file;
    private _shutdownOnce;
    constructor(file: string);
    /**
     * Export spans.
     * @param spans
     * @param resultCallback
     */
    export(spans: ReadableSpan[], resultCallback: (result: ExportResult) => void): void;
    /**
     * converts span info into more readable format
     * @param span
     */
    private _exportInfo;
    /**
     * Store spans in file
     * @param spans
     * @param done
     */
    private _sendSpans;
    forceFlush(): Promise<void>;
    /**
     * Shutdown the exporter.
     */
    shutdown(): Promise<void>;
    /**
     * Called by _shutdownOnce with BindOnceFuture
     */
    private _shutdown;
    private _flushAll;
}
//# sourceMappingURL=FileSpanExporter.d.ts.map